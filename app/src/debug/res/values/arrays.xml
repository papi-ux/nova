<?xml version="1.0" encoding="utf-8"?>
<!--
  The codec picker, with one extra entry.

  A debug source set replaces an array of the same name rather than adding to it, so these are the
  release entries plus PyroWave. It lives here because the entry is the switch: a release build has
  no way to select the codec, and PreferenceConfiguration ignores the value even if it finds it.
-->
<resources>
    <string-array name="video_format_names">
        <item>@string/videoformat_auto</item>
        <item>@string/videoformat_av1always</item>
        <item>@string/videoformat_hevcalways</item>
        <item>@string/videoformat_h264always</item>
        <item>@string/videoformat_pyrowave</item>
    </string-array>
    <string-array name="video_format_values" translatable="false">
        <item>auto</item>
        <item>forceav1</item>
        <item>forceh265</item>
        <item>neverh265</item>
        <item>forcepyrowave</item>
    </string-array>
</resources>
