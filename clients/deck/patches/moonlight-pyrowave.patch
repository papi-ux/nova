--- a/src/Limelight.h
+++ b/src/Limelight.h
@@ -220,0 +221 @@
+#define VIDEO_FORMAT_PYROWAVE        0x10000 // Nova/Polaris pinned SDR420 extension
@@ -501,0 +503 @@
+#define SCM_PYROWAVE        0x00800000 // Nova/Polaris pinned SDR420 extension
--- a/src/RtspConnection.c
+++ b/src/RtspConnection.c
@@ -2,0 +3 @@
+#include "NovaPyrowaveProtocol.h"
@@ -1076 +1077,10 @@
-        if ((StreamConfig.supportedVideoFormats & VIDEO_FORMAT_MASK_AV1) && strstr(response.payload, "AV1/90000")) {
+        if (StreamConfig.supportedVideoFormats & VIDEO_FORMAT_PYROWAVE) {
+            if (!(serverInfo->serverCodecModeSupport & SCM_PYROWAVE) ||
+                !nova_pyrowave_description_compatible(response.payload)) {
+                Limelog("Host does not offer the required PyroWave bitstream revision\n");
+                ret = -1;
+                goto Exit;
+            }
+            NegotiatedVideoFormat = VIDEO_FORMAT_PYROWAVE;
+        }
+        else if ((StreamConfig.supportedVideoFormats & VIDEO_FORMAT_MASK_AV1) && strstr(response.payload, "AV1/90000")) {
--- a/src/SdpGenerator.c
+++ b/src/SdpGenerator.c
@@ -433 +433,4 @@
-        if (NegotiatedVideoFormat & VIDEO_FORMAT_MASK_AV1) {
+        if (NegotiatedVideoFormat == VIDEO_FORMAT_PYROWAVE) {
+            err |= addAttributeString(&optionHead, "x-nv-vqos[0].bitStreamFormat", "3");
+        }
+        else if (NegotiatedVideoFormat & VIDEO_FORMAT_MASK_AV1) {
--- a/src/VideoDepacketizer.c
+++ b/src/VideoDepacketizer.c
@@ -215 +215 @@
-        else if (NegotiatedVideoFormat & VIDEO_FORMAT_MASK_AV1) {
+        else if (NegotiatedVideoFormat & (VIDEO_FORMAT_MASK_AV1 | VIDEO_FORMAT_PYROWAVE)) {
